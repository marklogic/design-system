import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import PropsPanels from './PropsPanels.jsx'
import MLDatePicker from '../src/MLDatePicker';

# MLDatePicker

[Ant Documentation](https://3x.ant.design/components/date-picker)

Note: On the hosted (non-local) StoryBook, this component's custom CSS is not currently rendering correctly. This should not affect use of the component in your app.


<PropsPanels of={[MLDatePicker, MLDatePicker.MLRangePicker]} />
