module.exports = {
  "rules": {
    // Allow unused vars for uniformity's sake
    "no-unused-vars": "off",
    "react/prop-types": "off", // Ignore complaining about `props` variable in stories
  }
}
