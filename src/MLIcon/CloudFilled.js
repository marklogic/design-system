import { createWrappedMLIcon } from './icon-wrappers'
import { default as AntCloudFilled } from '@ant-design/icons/CloudFilled'
const CloudFilled = createWrappedMLIcon(AntCloudFilled)
export default CloudFilled
