import { createWrappedMLIcon } from './icon-wrappers'
import { default as AntEyeFilled } from '@ant-design/icons/EyeFilled'
const EyeFilled = createWrappedMLIcon(AntEyeFilled)
export default EyeFilled
