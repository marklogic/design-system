import { createWrappedMLIcon } from './icon-wrappers'
import { default as AntBuildFilled } from '@ant-design/icons/BuildFilled'
const BuildFilled = createWrappedMLIcon(AntBuildFilled)
export default BuildFilled
