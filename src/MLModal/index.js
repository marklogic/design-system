import MLModal from './MLModal'

import {
  DraggableModalProvider,
} from 'ant-design-draggable-modal'

DraggableModalProvider.displayName = 'MLModal.MLDraggableModalProvider'

MLModal.MLDraggableModalProvider = DraggableModalProvider

export default MLModal
